# SRAMS → CAMIS Clone: Remaining Work Plan

> **Goal:** Complete remaining CAMIS clone phases
> **Status:** Phases 1-2 DONE (design system, UI components, exam distribution)
> **Remaining:** 7 backend controllers + 7 frontend pages + 4 legacy component migrations + database cleanup

---

## COMPLETED (Do NOT repeat)
- tailwind.config.js with CAMIS colors (#0077C1 blue, #FFCF00 yellow)
- index.css with shadcn/ui CSS variables
- lib/utils.js with cn()
- All 16 UI components rewritten to shadcn/ui pattern (Button 11 variants, Dialog Radix, etc.)
- Exam distribution: controller, route, frontend page, App.jsx route, sidebar item
- Database migration 006 with 9 new tables

---

## REMAINING WORK

### 1. Backend Controllers (6 new)
All follow the same pattern as examDistribution.controller.js.

**File:** `backend/controllers/curriculum.controller.js`
- getAll (filter by subject_id, class_id, type)
- create, update, remove
- getMaterials (curriculum_materials)

**File:** `backend/controllers/imihigo.controller.js`
- getAll (filter by status, period)
- create, update, remove
- getVideos (imihigo_videos)

**File:** `backend/controllers/submission.controller.js`
- getAll (filter by entity_type: 'sei'|'district', academic_year_id, term_id)
- create, update, remove

**File:** `backend/controllers/reportCardVerification.controller.js`
- getAll (filter by status)
- verify (set status='verified', verified_by, verified_at)
- reject (set status='rejected', comments)

**File:** `backend/controllers/teacherPerformance.controller.js`
- getAll (filter by teacher_id, academic_year_id, term_id)
- create, update

**File:** `backend/controllers/support.controller.js`
- getAll (filter by status, user_id)
- create, update (status, assigned_to)

### 2. Backend Routes (6 new)
**Files:** `backend/routes/router/curriculum.route.js`, `imihigo.route.js`, `submission.route.js`, `reportCardVerification.route.js`, `teacherPerformance.route.js`, `support.route.js`

### 3. Route Registration
**File:** `backend/routes/index.js`
- Import and mount all 6 new routes:
  - `/curriculum`, `/imihigo`, `/submissions`, `/report-card-verifications`, `/teacher-performance`, `/support`

### 4. Frontend Pages (7 new)
All use React Query + shadcn/ui components.

**File:** `frontend/src/pages/curriculum/Curriculum.jsx`
- CRUD for curriculum items
- Material list per curriculum

**File:** `frontend/src/pages/imihigo/ImihigoDashboard.jsx`
- Imihigo list with progress bars
- Video management

**File:** `frontend/src/pages/submissions/Submissions.jsx`
- SEI and District submission progress
- Progress bars, status badges

**File:** `frontend/src/pages/admin/ReportCardVerify.jsx`
- Report card verification queue
- Verify/reject with comments

**File:** `frontend/src/pages/teacher/TeacherPerformance.jsx`
- Teacher performance metrics
- Rating display

**File:** `frontend/src/pages/support/SupportTickets.jsx`
- Support ticket CRUD
- Status management, assignment

**File:** `frontend/src/pages/exams/ExamDistribution.jsx`
- Distribute papers to classrooms
- Track distribution status (pending/distributed/returned)

### 5. App.jsx Route Additions
Add lazy imports and routes for all 7 new pages under appropriate role routes.

### 6. AdminLayout Sidebar Additions
Add entries for: Curriculum, Imihigo, Submissions, Support Tickets

### 7. Legacy Component Migration (4 files)
Convert these to shadcn/ui pattern:
- `Modal.jsx` → Re-export from Dialog.jsx (wrap with Dialog/DialogContent)
- `ConfirmDialog.jsx` → Use AlertDialog from Radix
- `PageHeader.jsx` → Use cn() utility
- `StatCard.jsx` → Use cn() utility

### 8. Database Cleanup (Phase 7)
**Migration 007:** Drop unwanted tables:
```sql
DROP TABLE IF EXISTS student_transfers CASCADE;
DROP TABLE IF EXISTS medical_records CASCADE;
DROP TABLE IF EXISTS discipline_records CASCADE;
DROP TABLE IF EXISTS combination_subjects CASCADE;
DROP TABLE IF EXISTS subject_combinations CASCADE;
DROP TABLE IF EXISTS library_books CASCADE;
DROP TABLE IF EXISTS library_transactions CASCADE;
DROP TABLE IF EXISTS fee_structures CASCADE;
DROP TABLE IF EXISTS fee_payments CASCADE;
DROP TABLE IF EXISTS student_documents CASCADE;
```
Also drop corresponding controllers and routes that reference these tables.

---

## Execution Order
```
Step 1: Create 6 backend controllers + 6 routes + register in index.js
Step 2: Create 7 frontend pages
Step 3: Update App.jsx with routes + AdminLayout sidebar
Step 4: Migrate 4 legacy components
Step 5: Create migration 007 + drop unwanted tables
Step 6: Verify build
```

## Verification
1. `cd backend && node server.js` — no errors, migration 006+007 apply
2. `cd frontend && npm run build` — no errors
3. All new routes accessible: /curriculum, /imihigo, /submissions, /support, /exam-papers/distribute
4. Sidebar shows all new menu items
5. Dark mode works on all new pages

### Current Tables (37 tables in schema.sql + 10 in migration 005)

**Current table list:**
`roles`, `trade`, `users`, `academic_years`, `terms`, `classes`, `admission_sequences`, `students`, `subjects`, `teachers`, `teacher_subjects`, `assessments`, `grading_system`, `marks`, `attendance`, `report_cards`, `student_promotions`, `notifications`, `database_backups`, `school_profile`, `password_history`, `audit_logs`, `user_sessions`, `otp_codes`, `streams`, `departments`, `classrooms`, `timetables`, `subject_combinations`, `combination_subjects`, `student_transfers`, `discipline_records`, `medical_records`, `competencies`, `certificates`, `student_documents`, `report_card_templates`, `file_uploads`, `calendar_events`, `announcements`, `system_settings`, `parent_students`, `messages`, `fee_structures`, `fee_payments`, `library_books`, `library_transactions`

### Tables to DROP (not needed for CAMIS)
These tables exist but don't match CAMIS functionality:

```sql
DROP TABLE IF EXISTS student_transfers CASCADE;
DROP TABLE IF EXISTS medical_records CASCADE;
DROP TABLE IF EXISTS discipline_records CASCADE;
DROP TABLE IF EXISTS combination_subjects CASCADE;
DROP TABLE IF EXISTS subject_combinations CASCADE;
DROP TABLE IF EXISTS library_books CASCADE;
DROP TABLE IF EXISTS library_transactions CASCADE;
DROP TABLE IF EXISTS fee_structures CASCADE;
DROP TABLE IF EXISTS fee_payments CASCADE;
DROP TABLE IF EXISTS student_documents CASCADE;
```

### Tables to KEEP (match CAMIS core)
| Table | Purpose |
|-------|---------|
| `roles` | User roles (15 roles) |
| `trade` | Academic trades/departments |
| `users` | All system users |
| `academic_years` | Academic years |
| `terms` | Terms within years |
| `classes` | Classes per trade |
| `students` | Student records |
| `subjects` | Subjects per class |
| `teachers` | Teacher records |
| `teacher_subjects` | Teacher-class-subject allocations |
| `assessments` | Assessments/exams |
| `grading_system` | Grade boundaries |
| `marks` | Student marks with workflow |
| `attendance` | Daily attendance |
| `report_cards` | Generated report cards |
| `student_promotions` | Student promotions |
| `notifications` | User notifications |
| `school_profile` | School information |
| `password_history` | Password audit |
| `audit_logs` | Full audit trail |
| `user_sessions` | Active sessions |
| `otp_codes` | OTP verification |
| `streams` | Academic streams |
| `departments` | School departments |
| `classrooms` | Physical classrooms |
| `timetables` | Class schedules |
| `certificates` | Issued certificates |
| `report_card_templates` | Report card layouts |
| `file_uploads` | File management |
| `calendar_events` | School calendar |
| `announcements` | School announcements |
| `system_settings` | System configuration |
| `parent_students` | Parent-student linking |
| `messages` | Internal messaging |

### NEW Tables for CAMIS Features

```sql
-- Exam Distribution (CAMIS core feature)
CREATE TABLE IF NOT EXISTS exam_papers (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  exam_type VARCHAR(50) NOT NULL, -- 'mock', 'practical', 'national', 'continuous'
  title VARCHAR(200) NOT NULL,
  subject_id UUID REFERENCES subjects(id),
  class_id UUID REFERENCES classes(id),
  academic_year_id UUID REFERENCES academic_years(id),
  term_id UUID REFERENCES terms(id),
  total_marks INT DEFAULT 100,
  duration_minutes INT,
  status VARCHAR(30) DEFAULT 'draft', -- 'draft','published','distributed','completed'
  created_by UUID REFERENCES users(id),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS exam_distributions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  exam_paper_id UUID REFERENCES exam_papers(id),
  classroom_id UUID REFERENCES classrooms(id),
  distributed_to UUID REFERENCES users(id),
  distributed_at TIMESTAMP,
  returned_at TIMESTAMP,
  status VARCHAR(30) DEFAULT 'pending', -- 'pending','distributed','returned'
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Curriculum Management
CREATE TABLE IF NOT EXISTS curriculum (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(200) NOT NULL,
  type VARCHAR(50) NOT NULL, -- 'course_grade', 'legra', 'standard'
  subject_id UUID REFERENCES subjects(id),
  class_id UUID REFERENCES classes(id),
  description TEXT,
  created_by UUID REFERENCES users(id),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS curriculum_materials (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  curriculum_id UUID REFERENCES curriculum(id),
  title VARCHAR(200) NOT NULL,
  material_type VARCHAR(50), -- 'textbook', 'workbook', 'digital', 'video'
  file_url TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Imihigo (Performance Contracts)
CREATE TABLE IF NOT EXISTS imihigo (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  title VARCHAR(200) NOT NULL,
  description TEXT,
  target_value NUMERIC(10,2),
  actual_value NUMERIC(10,2) DEFAULT 0,
  status VARCHAR(30) DEFAULT 'active', -- 'active','completed','expired'
  period VARCHAR(50), -- 'term1_2026', 'annual_2026'
  created_by UUID REFERENCES users(id),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS imihigo_videos (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  imihigo_id UUID REFERENCES imihigo(id),
  title VARCHAR(200),
  video_url TEXT NOT NULL,
  description TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Submission Progress (SEI/District)
CREATE TABLE IF NOT EXISTS submission_progress (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  entity_type VARCHAR(50) NOT NULL, -- 'sei', 'district'
  entity_name VARCHAR(200) NOT NULL,
  academic_year_id UUID REFERENCES academic_years(id),
  term_id UUID REFERENCES terms(id),
  total_items INT DEFAULT 0,
  submitted_items INT DEFAULT 0,
  status VARCHAR(30) DEFAULT 'pending', -- 'pending','in_progress','complete'
  deadline DATE,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Report Card Verification
CREATE TABLE IF NOT EXISTS report_card_verifications (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  report_card_id UUID REFERENCES report_cards(id),
  verified_by UUID REFERENCES users(id),
  verified_at TIMESTAMP,
  status VARCHAR(30) DEFAULT 'pending', -- 'pending','verified','rejected'
  comments TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Teacher Performance Audit
CREATE TABLE IF NOT EXISTS teacher_performance (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  teacher_id UUID REFERENCES teachers(id),
  academic_year_id UUID REFERENCES academic_years(id),
  term_id UUID REFERENCES terms(id),
  avg_student_marks NUMERIC(5,2),
  attendance_rate NUMERIC(5,2),
  completion_rate NUMERIC(5,2),
  rating VARCHAR(20), -- 'excellent','good','satisfactory','needs_improvement'
  reviewed_by UUID REFERENCES users(id),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Support Tickets
CREATE TABLE IF NOT EXISTS support_tickets (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id),
  subject VARCHAR(200) NOT NULL,
  description TEXT NOT NULL,
  priority VARCHAR(20) DEFAULT 'normal', -- 'low','normal','high','urgent'
  status VARCHAR(20) DEFAULT 'open', -- 'open','in_progress','resolved','closed'
  assigned_to UUID REFERENCES users(id),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Indexes for new tables
CREATE INDEX IF NOT EXISTS idx_exam_papers_subject ON exam_papers(subject_id);
CREATE INDEX IF NOT EXISTS idx_exam_papers_class ON exam_papers(class_id);
CREATE INDEX IF NOT EXISTS idx_exam_papers_year ON exam_papers(academic_year_id);
CREATE INDEX IF NOT EXISTS idx_exam_distributions_paper ON exam_distributions(exam_paper_id);
CREATE INDEX IF NOT EXISTS idx_curriculum_subject ON curriculum(subject_id);
CREATE INDEX IF NOT EXISTS idx_curriculum_class ON curriculum(class_id);
CREATE INDEX IF NOT EXISTS idx_imihigo_status ON imihigo(status);
CREATE INDEX IF NOT EXISTS idx_submission_progress_entity ON submission_progress(entity_type);
CREATE INDEX IF NOT EXISTS idx_teacher_performance_teacher ON teacher_performance(teacher_id);
CREATE INDEX IF NOT EXISTS idx_support_tickets_user ON support_tickets(user_id);
CREATE INDEX IF NOT EXISTS idx_support_tickets_status ON support_tickets(status);
```

---

## PART 2: CAMIS Design System

### Color Tokens (shadcn/ui pattern)
```css
:root {
  --background: 0 0% 100%;
  --foreground: 222.2 84% 4.9%;
  --primary: 200 100% 38%; /* #0077C1 CAMIS Blue */
  --primary-foreground: 0 0% 98%;
  --secondary: 48 100% 50%; /* #FFCF00 CAMIS Yellow */
  --secondary-foreground: 222.2 47.4% 11.2%;
  --destructive: 0 84.2% 60.2%;
  --destructive-foreground: 210 40% 98%;
  --success: 142 76% 36%;
  --success-foreground: 0 0% 98%;
  --muted: 210 40% 96.1%;
  --muted-foreground: 215.4 16.3% 46.9%;
  --accent: 210 40% 96.1%;
  --accent-foreground: 222.2 47.4% 11.2%;
  --border: 214.3 31.8% 91.4%;
  --input: 214.3 31.8% 91.4%;
  --ring: 200 100% 38%;
  --radius: 0.5rem;
}
.dark {
  --background: 222.2 84% 4.9%;
  --foreground: 210 40% 98%;
  --primary: 200 100% 50%;
  --primary-foreground: 222.2 47.4% 11.2%;
  --secondary: 48 100% 50%;
  --secondary-foreground: 222.2 47.4% 11.2%;
  --muted: 217.2 32.6% 17.5%;
  --muted-foreground: 215 20.2% 65.1%;
  --accent: 217.2 32.6% 17.5%;
  --border: 217.2 32.6% 17.5%;
  --input: 217.2 32.6% 17.5%;
  --ring: 200 100% 50%;
}
```

### Button Variants (11 variants)
```javascript
variants: {
  variant: {
    default: "bg-primary text-primary-foreground shadow-xs hover:bg-primary/90",
    "primary-light": "bg-blue-100 text-primary shadow-xs hover:bg-primary/40",
    success: "bg-green-600 hover:bg-green-400 text-white shadow-xs",
    "success-light": "bg-emerald-100 hover:bg-green-400 text-green-700",
    destructive: "bg-destructive text-white shadow-xs hover:bg-destructive/90",
    "destructive-light": "bg-red-100 hover:bg-red-400 text-red-700",
    outline: "border bg-background shadow-xs hover:bg-accent hover:text-accent-foreground",
    secondary: "bg-yellow-400 hover:bg-yellow-500 text-secondary-foreground shadow-xs",
    "secondary-light": "bg-yellow-200 hover:bg-yellow-500 text-yellow-800",
    ghost: "hover:bg-accent hover:text-accent-foreground",
    link: "text-primary underline-offset-4 hover:underline",
  },
  size: {
    default: "h-9 px-4 py-2 has-[>svg]:px-3",
    xs: "h-6 rounded-md gap-1.5 px-2 text-sm",
    sm: "h-8 rounded-md gap-1.5 px-3",
    lg: "h-10 rounded-md px-6",
    icon: "size-9",
  },
}
```

---

## PART 3: Frontend Rebuild — Phase by Phase

### PHASE 1: Design System & Core Infrastructure
**Files:** ~5 modified, ~3 new

1. Rewrite `tailwind.config.js` with CAMIS color tokens
2. Rewrite `index.css` with shadcn/ui CSS variables
3. Create `lib/utils.js` with `cn()` helper
4. Install shadcn/ui dependencies: `@radix-ui/*`, `class-variance-authority`, `clsx`, `tailwind-merge`
5. Update `package.json` dependencies

### PHASE 2: shadcn/ui Components (16 rewrite + 7 new)
**Files:** ~23

Rewrite all existing UI components to shadcn/ui pattern:
- Button (11 variants), Card (with sub-components), Input, Select, Dialog, Badge, Alert, Avatar, Breadcrumb, Skeleton, Pagination, SearchInput, ConfirmDialog, EmptyState, StatCard, PageHeader

Create new components:
- Sheet, Tabs, Switch, Progress, Tooltip, DropdownMenu, Command

### PHASE 3: Layouts (5 rewrite)
**Files:** 5

Rewrite all layouts to match CAMIS:
- Collapsible sidebar with blue header, yellow active state
- Topbar with glass effect, notifications, theme toggle
- Mobile: Sheet slide-out sidebar
- Shared sidebar/topbar components

### PHASE 4: Auth Pages (6 rewrite)
**Files:** 6

Match CAMIS auth UI:
- Clean centered card, portal toggle
- OTP input, security questions, password hints

### PHASE 5: Dashboard & Analytics
**Files:** ~5

- Gauge charts, completion rates
- Teacher performance tracking
- District/SEI submission progress

### PHASE 6: New CAMIS Modules
**Files:** ~20 new pages + ~10 new backend files

**Exam Distribution:** Exam papers, mock exams, practical exams, paper distribution, timetable, audit logs
**Curriculum:** Course grade curriculum, Legra curriculum, materials
**Imihigo:** Performance contracts, videos
**Submissions:** SEI progress, district progress
**Report Cards:** Add verification, printing
**Teacher Performance:** Metrics, audit trail
**Support:** System support tickets

### PHASE 7: Database Migration
**Files:** 1 new migration + schema.sql rewrite

- Migration 006: Create new CAMIS tables
- Rewrite schema.sql: Drop unwanted tables, add CAMIS tables
- Clean up Neon database: DROP unwanted tables

### PHASE 8: Offline Support
**Files:** ~3

- OfflineIndicator component
- Service worker
- Cached responses

---

## PART 4: Backend Changes

### New Controllers
- `examDistribution.controller.js` — Exam papers, distribution, timetable
- `curriculum.controller.js` — Curriculum CRUD
- `imihigo.controller.js` — Performance contracts
- `submission.controller.js` — SEI/district progress
- `teacherPerformance.controller.js` — Teacher metrics
- `support.controller.js` — Support tickets

### New Routes
- `/api/v1/exam-papers` — Exam paper CRUD
- `/api/v1/exam-distributions` — Distribution tracking
- `/api/v1/curriculum` — Curriculum management
- `/api/v1/imihigo` — Imihigo CRUD
- `/api/v1/submissions` — Submission progress
- `/api/v1/teacher-performance` — Performance metrics
- `/api/v1/support` — Support tickets

### Schema Cleanup Migration
```sql
-- Drop unwanted tables
DROP TABLE IF EXISTS student_transfers CASCADE;
DROP TABLE IF EXISTS medical_records CASCADE;
DROP TABLE IF EXISTS discipline_records CASCADE;
DROP TABLE IF EXISTS combination_subjects CASCADE;
DROP TABLE IF EXISTS subject_combinations CASCADE;
DROP TABLE IF EXISTS library_books CASCADE;
DROP TABLE IF EXISTS library_transactions CASCADE;
DROP TABLE IF EXISTS fee_structures CASCADE;
DROP TABLE IF EXISTS fee_payments CASCADE;
DROP TABLE IF EXISTS student_documents CASCADE;

-- Create new CAMIS tables
-- (see Part 1 above for full CREATE TABLE statements)
```

---

## Execution Order

```
Phase 1 (Design System) 
  → Phase 2 (UI Components) 
    → Phase 3 (Layouts) 
      → Phase 4 (Auth) 
        → Phase 5 (Dashboard) 
          → Phase 6 (New Modules) 
            → Phase 7 (Database) 
              → Phase 8 (Offline)
```

## Verification
1. Visual: CAMIS at camis.nesa.gov.rw alongside SRAMS
2. Colors: Primary blue #0077C1, secondary yellow #FFCF00
3. Components: shadcn/ui patterns throughout
4. Dark mode: Works on all pages
5. Build: `npm run build` succeeds
6. Database: Clean schema with only needed tables
7. All existing functionality preserved
